# Exact rebound-component proof experiment

This candidate reproduces the historical pure exact-proof patch on top of the
immutable `rank_5` bot. It reconstructs a current-turn route to the attacking
goal when one exists entirely through rebound vertices, and proves a closed
component loss under the contest blocked-mover rule.

The candidate is exact and locally strong, but it was rejected by the
predeclared validation color guard. See `EXPERIMENTS.md`; do not submit it.
